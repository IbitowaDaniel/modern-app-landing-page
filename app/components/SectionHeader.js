

const SectionHeader = ({ heading, headingSecondary }) => (
  <div className="text-center">
    <p className="sectionTextHeading">{heading}</p>
    <h3 className="sectionTextHeadingSecondary">{headingSecondary}</h3>
  </div>
);


export default SectionHeader